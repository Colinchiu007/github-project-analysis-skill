# 蚁小二 4.0 逆向工程分析报告

> **分析时间**：2026-06-29
> **产品版本**：4.13.19
> **安装路径**：D:\Program Files\yixiaoer

---

## 一、产品概况

| 指标 | 值 |
|------|-----|
| **产品名称** | 蚁小二 4.0 |
| **版本** | 4.13.19 |
| **技术栈** | Electron + React 19 + TypeScript |
| **文件大小** | ~206 MB（主程序） |
| **更新地址** | https://lite-download.yixiaoer.cn/latest-open |
| **支持语言** | 中文、英文 |

---

## 二、技术架构

### 2.1 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    蚁小二 4.0 (Electron)                     │
├─────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │   Main      │  │   Preload    │  │    Renderer      │   │
│  │  (主进程)   │  │  (预加载)    │  │   (渲染进程)     │   │
│  │  8.4MB      │  │  40KB        │  │   126KB          │   │
│  └─────────────┘  └──────────────┘  └──────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                    核心功能模块                               │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │  小红书API  │  │  抖音API     │  │  视频号API       │   │
│  │  (完整集成) │  │  (完整集成)  │  │  (完整集成)      │   │
│  └─────────────┘  └──────────────┘  └──────────────────┘   │
│  ┌─────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │  B站API     │  │  微博API     │  │  知乎API         │   │
│  │  (完整集成) │  │  (完整集成)  │  │  (完整集成)      │   │
│  └─────────────┘  └──────────────┘  └──────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                    通信层                                    │
│  ┌─────────────────────────────────────────────────────┐    │
│  │              Socket.IO (实时通信)                     │    │
│  └─────────────────────────────────────────────────────┘    │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 目录结构

```
yixiaoer/
├── packages/
│   ├── main/                    # Electron 主进程
│   │   └── dist/
│   │       ├── index.cjs        # 主入口 (8.4MB)
│   │       ├── icon.png         # 应用图标
│   │       ├── tray/            # 系统托盘图标
│   │       └── client-dist/     # Socket.IO 客户端
│   ├── preload/                 # 预加载脚本
│   │   └── dist/
│   │       └── index.cjs        # 预加载入口
│   └── renderer/                # 渲染进程
│       └── dist/
│           ├── index.html       # 主页面
│           ├── main-DK0VFnck.js # 主 JS (126KB)
│           ├── collect.html     # 采集页面
│           ├── online.html      # 在线状态页面
│           ├── tool.html        # 工具页面
│           ├── update.html      # 更新页面
│           └── *.js             # 其他模块
├── locales/                     # 语言包
│   ├── zh-CN.pak               # 中文
│   └── en-US.pak               # 英文
└── resources/
    ├── app.asar                 # Electron 应用包
    ├── app-update.yml           # 更新配置
    └── elevate.exe              # 提权工具
```

---

## 三、核心功能

### 3.1 多平台发布

| 平台 | 功能 | 状态 |
|------|------|------|
| **小红书** | 笔记发布、视频上传、数据分析、好友管理 | ✅ 完整 |
| **抖音** | 视频发布、图文发布 | ✅ 完整 |
| **视频号** | 视频发布、图文发布 | ✅ 完整 |
| **B站** | 视频发布、专栏发布 | ✅ 完整 |
| **微博** | 图文发布 | ✅ 完整 |
| **知乎** | 文章发布 | ✅ 完整 |
| **公众号** | 文章发布 | ✅ 完整 |

### 3.2 小红书 API 集成

从代码中提取的 API 端点：

```javascript
// 内容管理
- /web_api/sns/v5/creator/note/user/posted     // 获取已发布笔记
- /web_api/sns/capa/postgw/note/delete         // 删除笔记
- /api/galaxy/creator/datacenter/note/analyze/list  // 数据分析

// 用户信息
- /web_api/sns/v5/user/info                    // 获取用户信息
- /web_api/sns/v5/user/friend/list             // 获取好友列表

// 位置和话题
- /web_api/sns/v5/poi/list                     // 获取位置列表
- /web_api/sns/v5/topic/list                   // 获取话题列表

// 视频上传
- https://{uploadAddr}/{fileId}?uploads        // 初始化上传
- https://{uploadAddr}/{fileId}?partNumber=N   // 分片上传
```

### 3.3 视频上传流程

```javascript
// 1. 获取上传 ID
const uploadId = await getUploadIdResponse({
    uploadAddr: "xxx",
    fileIds: ["xxx"],
    token: "xxx"
});

// 2. 分片上传视频
for (let i = 0; i < chunks.length; i++) {
    await uploadVideoPart(chunks[i], i + 1, uploadInfo, uploadId);
}

// 3. 完成上传
await getUploadedVideoEtag(uploadInfo, uploadId, etagMap);
```

### 3.4 数据分析功能

支持的分析维度：

| 维度 | 说明 |
|------|------|
| 总粉丝数 | 账号粉丝总数 |
| 总获赞与收藏 | 点赞和收藏总数 |
| 观看次数 | 内容被观看的次数 |
| 观看总时长 | 内容被观看的总时长（小时） |
| 主页访客 | 访问主页的用户数 |
| 点赞 | 点赞次数 |
| 收藏 | 收藏次数 |
| 评论 | 评论次数 |
| 弹幕 | 弹幕次数 |
| 笔记涨粉 | 通过笔记新增的粉丝 |
| 笔记分享 | 笔记被分享的次数 |

### 3.5 实时通信

使用 Socket.IO 进行实时状态同步：

```javascript
// 服务端
io.on('connection', (socket) => {
    socket.emit('sync-content', data);
    socket.emit('publish-status', status);
});

// 客户端
socket.on('sync-content', (data) => { /* 更新内容列表 */ });
socket.on('publish-status', (status) => { /* 更新发布状态 */ });
```

---

## 四、依赖分析

### 4.1 核心依赖

| 依赖 | 版本 | 用途 |
|------|------|------|
| **React** | ^19.1.0 | UI 框架 |
| **Socket.IO** | ^4.8.1 | 实时通信 |
| **Socket.IO Client** | ^4.8.1 | 客户端通信 |
| **Axios** | ^1.10.0 | HTTP 请求 |
| **Zod** | ^4.3.5 | 数据验证 |
| **Day.js** | ^1.11.12 | 日期处理 |
| **Lucide React** | ^0.517.0 | 图标库 |
| **Tailwind Merge** | ^3.3.1 | CSS 类合并 |
| **@hello-pangea/dnd** | ^18.0.1 | 拖拽排序 |
| **Nanoid** | ^3.3.11 | ID 生成 |

### 4.2 架构特点

| 特点 | 说明 |
|------|------|
| **Monorepo** | 使用 pnpm workspaces |
| **前端** | React 19 + Vite |
| **后端** | Electron Main Process |
| **通信** | Socket.IO |
| **构建** | Webpack (主进程) + Vite (渲染进程) |

---

## 五、与 Multi-Publish 的对比

| 维度 | 蚁小二 4.0 | Multi-Publish |
|------|-----------|---------------|
| **版本** | 4.13.19 | v1.2.0 |
| **平台支持** | 7+ 平台 | 12 平台 |
| **技术栈** | React 19 + Electron | Vue 3 + Electron |
| **实时通信** | ✅ Socket.IO | ❌ 无 |
| **数据分析** | ✅ 完整 | ⚠️ 部分 |
| **视频上传** | ✅ 分片上传 | ✅ 基础上传 |
| **代码质量** | ⭐⭐⭐⭐ | ⭐⭐⭐ |

---

## 六、可借鉴的功能

### 1. Socket.IO 实时通信

蚁小二使用 Socket.IO 进行实时状态同步，可以借鉴到 Multi-Publish：

```javascript
// 实时发布状态同步
socket.emit('publish-status', { platform, status, progress });
socket.on('publish-complete', (data) => { /* 更新 UI */ });
```

### 2. 分片视频上传

蚁小二的视频上传支持分片上传，可以借鉴到 Story2Video：

```javascript
// 分片上传流程
1. 获取上传 ID
2. 分片上传（每片 5MB）
3. 合并分片
4. 返回视频 URL
```

### 3. 数据分析仪表板

蚁小二的数据分析功能很完整，可以借鉴到 trendscope：

```javascript
// 数据分析维度
- 粉丝增长趋势
- 内容互动数据
- 平台对比分析
```

### 4. 代理支持

蚁小二支持 HTTP 代理，可以借鉴到 Multi-Publish：

```javascript
// 代理配置
const proxy = {
    serverAdd: 'http://proxy:port',
    // 快代理配置
};
```

---

## 七、安全分析

| 风险 | 说明 | 建议 |
|------|------|------|
| **Cookie 存储** | 使用本地存储，可能有安全风险 | 使用加密存储 |
| **API Key** | 硬编码在代码中（已混淆） | 使用环境变量 |
| **代理支持** | 支持 HTTP 代理，可能用于绕过限制 | 监控代理使用 |
| **数据传输** | 使用 HTTP 明文传输敏感数据 | 使用 HTTPS |

---

## 八、总结

蚁小二是一个**功能完整的多平台内容发布工具**，主要特点：

1. **多平台支持**：7+ 平台完整集成
2. **实时通信**：Socket.IO 实时状态同步
3. **数据分析**：完整的数据仪表板
4. **视频处理**：分片上传、格式转换

**与 Multi-Publish 的差异**：
- 蚁小二更专注于小红书等平台的深度集成
- Multi-Publish 支持更多平台但集成深度较浅
- 蚁小二有实时通信功能，Multi-Publish 没有

**建议借鉴**：
1. Socket.IO 实时通信机制
2. 分片视频上传方案
3. 数据分析仪表板设计
4. 代理支持配置

---

## 九、附录

### 9.1 文件清单

| 文件 | 大小 | 说明 |
|------|------|------|
| 蚁小二4.0.exe | 205MB | 主程序 |
| app.asar | 10.9MB | Electron 应用包 |
| index.cjs | 8.4MB | 主进程代码 |
| main-DK0VFnck.js | 126KB | 渲染进程主代码 |
| scrollBar-BP5m29SY.js | 187KB | 滚动条组件 |

### 9.2 语言支持

- 中文 (zh-CN)
- 英文 (en-US)

### 9.3 更新配置

```yaml
provider: generic
channel: win32-x64
url: https://lite-download.yixiaoer.cn/latest-open
updaterCacheDirName: yixiaoer-updater
```
