# 编辑器项目深度挖掘报告

> **分析时间**：2026-06-29
> **目标项目**：Multi-Publish（Electron + Vue 3 桌面应用）
> **分析项目数**：5 个（AFFiNE、Quill、Tiptap、WangEditor、doocs/md）
> **报告字数**：10,000+ 字

---

## 一、Tiptap 的自定义扩展开发架构

### 1.1 扩展架构设计

Tiptap 的核心设计哲学是 **"Headless"（无头）**，将编辑器逻辑与 UI 完全分离。其扩展系统基于 ProseMirror，提供了三种扩展类型：`Extension`、`Node`、`Mark`。

**Extension 基类设计：**

```typescript
// Extension 是最基础的扩展类型，不对应任何 ProseMirror Schema 节点
export const Extension = Extendable.create({
  name: 'extension',
  type: 'extension',
})
```

**Node 基类设计：**

```typescript
// Node 扩展用于定义文档中的块级或内联节点
export const Node = Extendable.create({
  name: 'node',
  type: 'node',
})
```

**Mark 基类设计：**

```typescript
// Mark 扩展用于定义文本的格式标记（如加粗、斜体、链接等）
export const Mark = Extendable.create({
  name: 'mark',
  type: 'mark',
})
```

### 1.2 Image Node 扩展完整示例

```typescript
import { mergeAttributes, Node, nodeInputRule } from '@tiptap/core'

export const Image = Node.create<ImageOptions>({
  name: 'image',

  addOptions() {
    return {
      inline: false,
      allowBase64: false,
      HTMLAttributes: {},
    }
  },

  inline() { return this.options.inline },
  group() { return this.options.inline ? 'inline' : 'block' },
  draggable: true,

  addAttributes() {
    return {
      src: { default: null },
      alt: { default: null },
      title: { default: null },
      width: { default: null },
      height: { default: null },
    }
  },

  parseHTML() {
    return [
      {
        tag: this.options.allowBase64
          ? 'img[src]'
          : 'img[src]:not([src^="data:"])',
      },
    ]
  },

  renderHTML({ HTMLAttributes }) {
    return ['img', mergeAttributes(this.options.HTMLAttributes, HTMLAttributes)]
  },

  addCommands() {
    return {
      setImage: options => ({ commands }) => {
        return commands.insertContent({ type: this.name, attrs: options })
      },
    }
  },

  addInputRules() {
    return [
      nodeInputRule({
        find: /(?:^|\s)(!\[(.+|:?)]\((\S+)(?:(?:\s+)["'](\S+)["'])?\))$/,
        type: this.type,
        getAttributes: match => {
          const [, , alt, src, title] = match
          return { src, alt, title }
        },
      }),
    ]
  },
})
```

### 1.3 Link Mark 扩展架构

```typescript
export const Link = Mark.create<LinkOptions>({
  name: 'link',
  priority: 1000,
  keepOnSplit: false,
  exitable: true,

  addAttributes() {
    return {
      href: { default: null },
      target: { default: this.options.HTMLAttributes.target ?? null },
      rel: { default: this.options.HTMLAttributes.rel ?? null },
      class: { default: this.options.HTMLAttributes.class ?? null },
      title: { default: null },
    }
  },

  parseHTML() {
    return [{
      tag: 'a[href]',
      getAttrs: dom => {
        const href = (dom as HTMLElement).getAttribute('href')
        if (!href || !this.options.isAllowedUri(href, {
          defaultValidate: url => !!isAllowedUri(url, this.options.protocols),
          protocols: this.options.protocols,
        })) {
          return false
        }
        return null
      },
    }]
  },

  renderHTML({ HTMLAttributes }) {
    return ['a', mergeAttributes(this.options.HTMLAttributes, HTMLAttributes), 0]
  },

  addCommands() {
    return {
      setLink: attributes => ({ chain }) => {
        return chain().setMark(this.name, attributes).setMeta('preventAutolink', true).run()
      },
      toggleLink: attributes => ({ chain }) => {
        return chain()
          .toggleMark(this.name, attributes, { extendEmptyMarkRange: true })
          .setMeta('preventAutolink', true)
          .run()
      },
      unsetLink: () => ({ chain }) => {
        return chain()
          .unsetMark(this.name, { extendEmptyMarkRange: true })
          .setMeta('preventAutolink', true)
          .run()
      },
    }
  },
})
```

---

## 二、Quill 的 Delta 数据格式与架构

### 2.1 Delta 操作定义

```typescript
// Delta 的三种基本操作类型
interface Op {
  insert?: string | object
  retain?: number | object
  delete?: number
}

// 示例：一段加粗文本 "Hello World"
const delta = new Delta()
  .insert('Hello ', { bold: true })
  .insert('World')
  .insert('\n', { header: 1 })
```

### 2.2 Quill 核心架构

```typescript
class Quill {
  static DEFAULTS = {
    bounds: null,
    modules: {
      clipboard: true,
      keyboard: true,
      history: true,
      uploader: true,
    },
    placeholder: '',
    readOnly: false,
    registry: globalRegistry,
    theme: 'default',
  }

  static register(targets, overwrite?: boolean): void {
    if (typeof args[0] !== 'string') {
      const target = args[0]
      const name = 'attrName' in target ? target.attrName : target.blotName
      if (typeof name === 'string') {
        this.register(`formats/${name}`, target, overwrite)
      }
    }
  }

  constructor(container: HTMLElement | string, options: QuillOptions = {}) {
    this.options = expandConfig(container, options)
    this.root = this.addContainer('ql-editor')
    this.emitter = new Emitter()
    this.scroll = new ScrollBlot(this.options.registry, this.root, { emitter: this.emitter })
    this.editor = new Editor(this.scroll)
    this.selection = new Selection(this.scroll, this.emitter)
    this.theme = new this.options.theme(this, this.options)
  }
}
```

### 2.3 Delta 到 HTML 转换

```typescript
const CLIPBOARD_CONFIG: [Selector, Matcher][] = [
  [Node.TEXT_NODE, matchText],
  [Node.TEXT_NODE, matchNewline],
  ['br', matchBreak],
  [Node.ELEMENT_NODE, matchNewline],
  [Node.ELEMENT_NODE, matchBlot],
  [Node.ELEMENT_NODE, matchAttributor],
  [Node.ELEMENT_NODE, matchStyles],
  ['li', matchIndent],
  ['ol, ul', matchList],
  ['pre', matchCodeBlock],
  ['tr', matchTable],
  ['b', createMatchAlias('bold')],
  ['i', createMatchAlias('italic')],
  ['strike', createMatchAlias('strike')],
]

function traverse(scroll, node, elementMatchers, textMatchers, nodeMatches): Delta {
  if (node.nodeType === node.TEXT_NODE) {
    return textMatchers.reduce((delta, matcher) => matcher(node, delta, scroll), new Delta())
  }
  if (node.nodeType === node.ELEMENT_NODE) {
    return Array.from(node.childNodes).reduce((delta, childNode) => {
      let childrenDelta = traverse(scroll, childNode, elementMatchers, textMatchers, nodeMatches)
      if (childNode.nodeType === node.ELEMENT_NODE) {
        childrenDelta = elementMatchers.reduce((reducedDelta, matcher) => {
          return matcher(childNode, reducedDelta, scroll)
        }, childrenDelta)
      }
      return delta.concat(childrenDelta)
    }, new Delta())
  }
  return new Delta()
}
```

---

## 三、doocs/md 的多图床管理与工具函数

### 3.1 通用文件下载函数

```typescript
export function downloadFile(
  content: string,
  filename: string,
  mimeType: string = 'text/plain'
) {
  if (typeof document === 'undefined') {
    throw new TypeError('downloadFile can only be used in browser environment')
  }

  const downLink = document.createElement('a')
  downLink.download = filename
  downLink.style.display = 'none'
  let objectUrl: string | null = null

  if (content.startsWith('data:') || content.startsWith('blob:')) {
    downLink.href = content
  } else if (mimeType === 'text/html') {
    downLink.href = `data:text/html;charset=utf-8,${encodeURIComponent(content)}`
  } else {
    const blob = new Blob([content], { type: mimeType })
    objectUrl = URL.createObjectURL(blob)
    downLink.href = objectUrl
  }

  document.body.appendChild(downLink)
  downLink.click()
  document.body.removeChild(downLink)

  if (objectUrl) URL.revokeObjectURL(objectUrl)
}
```

### 3.2 文件转 Base64

```typescript
export function toBase64(file: Blob): Promise<string> {
  return new Promise<string>((resolve, reject) => {
    const reader = new FileReader()
    reader.readAsDataURL(file)
    reader.onload = () => resolve(reader.result as string).split(',').pop()!)
    reader.onerror = error => reject(error)
  })
}
```

### 3.3 CJK 友好的阅读时间计算

```typescript
function isCJK(char: string | undefined): boolean {
  if (typeof char !== 'string') return false
  const charCode = char.charCodeAt(0)
  return codeIsInRanges(charCode, [
    [0x3040, 0x309F],   // 日文平假名
    [0x4E00, 0x9FFF],   // CJK 统一汉字
    [0xAC00, 0xD7A3],   // 韩文音节
    [0x20000, 0x2EBE0], // CJK 扩展 B-F
  ])
}

export default function readingTime(
  text: string,
  options: ReadingTimeOptions = {}
): ReadTimeResults {
  let words = 0
  let start = 0
  let end = text.length - 1
  const wordsPerMinute = options.wordsPerMinute || 200

  while (isWordBound(text[start])) start++
  while (isWordBound(text[end])) end--

  const normalizedText = `${text}\n`
  for (let index = start; index <= end; index++) {
    if (
      isCJK(normalizedText[index]) ||
      (!isWordBound(normalizedText[index]) &&
        (isWordBound(normalizedText[index + 1]) || isCJK(normalizedText[index + 1])))
    ) {
      words++
    }
  }

  const minutes = words / wordsPerMinute
  return {
    text: `${Math.ceil(Number(minutes.toFixed(2)))} min read`,
    minutes,
    time: Math.round(minutes * 60 * 1000),
    words,
  }
}
```

### 3.4 格式切换工具

```typescript
export function toggleFormat(view: EditorView, options: ToggleFormatOptions): void {
  const { prefix, suffix, check, afterInsertCursorOffset = 0 } = options
  const selection = view.state.selection.main
  const selected = view.state.doc.sliceString(selection.from, selection.to)
  const isFormatted = check?.(selected) ?? false

  if (isFormatted) {
    const newText = selected.slice(prefix.length, selected.length - suffix.length)
    view.dispatch(view.state.replaceSelection(newText))
  } else {
    const newText = `${prefix}${selected}${suffix}`
    view.dispatch(view.state.replaceSelection(newText))
    if (afterInsertCursorOffset !== 0) {
      const newPos = view.state.selection.main.head + afterInsertCursorOffset
      view.dispatch({ selection: { anchor: newPos } })
    }
  }
}

export function formatBold(view: EditorView) {
  toggleFormat(view, {
    prefix: '**', suffix: '**',
    check: s => s.startsWith('**') && s.endsWith('**'),
    afterInsertCursorOffset: -2,
  })
}

export function formatLink(view: EditorView) {
  toggleFormat(view, {
    prefix: '[', suffix: ']()',
    check: s => s.startsWith('[') && s.endsWith(']()'),
    afterInsertCursorOffset: -1,
  })
}
```

---

## 四、编辑器性能优化技术

### 4.1 增量更新优化

```typescript
// Quill 的单字符变更快速路径
update(change, mutations = [], selectionInfo): Delta {
  const oldDelta = this.delta
  if (
    mutations.length === 1 &&
    mutations[0].type === 'characterData' &&
    mutations[0].target.data.match(ASCII) &&
    this.scroll.find(mutations[0].target)
  ) {
    const textBlot = this.scroll.find(mutations[0].target)
    const formats = bubbleFormats(textBlot)
    const index = textBlot.offset(this.scroll)
    const oldValue = mutations[0].oldValue.replace(CursorBlot.CONTENTS, '')
    const oldText = new Delta().insert(oldValue)
    const newText = new Delta().insert(textBlot.value())
    change = new Delta()
      .retain(index)
      .concat(oldText.diff(newText, relativeSelectionInfo))
      .reduce((delta, op) => {
        if (op.insert) return delta.insert(op.insert, formats)
        return delta.push(op)
      }, new Delta())
    this.delta = oldDelta.compose(change)
  } else {
    this.delta = this.getDelta()
    if (!change || !isEqual(oldDelta.compose(change), this.delta)) {
      change = oldDelta.diff(this.delta, selectionInfo)
    }
  }
  return change
}
```

### 4.2 缓存策略

```typescript
// Block Blot 的惰性计算+缓存
class Block extends BlockBlot {
  cache: { delta?: Delta | null; length?: number } = {}

  delta(): Delta {
    if (this.cache.delta == null) {
      this.cache.delta = blockDelta(this)
    }
    return this.cache.delta
  }

  length() {
    if (this.cache.length == null) {
      this.cache.length = super.length() + NEWLINE_LENGTH
    }
    return this.cache.length
  }

  deleteAt() { super.deleteAt(...); this.cache = {} }
  formatAt() { super.formatAt(...); this.cache = {} }
  insertAt() { super.insertAt(...); this.cache = {} }
  optimize() { super.optimize(context); this.cache = {} }
}
```

### 4.3 事务批处理

```typescript
// Tiptap 的事务捕获机制
public captureTransaction(fn: () => void) {
  this.isCapturingTransaction = true
  fn()
  this.isCapturingTransaction = false
  const tr = this.capturedTransaction
  this.capturedTransaction = null
  return tr
}

private dispatchTransaction(transaction: Transaction): void {
  if (this.isCapturingTransaction) {
    if (!this.capturedTransaction) {
      this.capturedTransaction = transaction
      return
    }
    transaction.steps.forEach(step => this.capturedTransaction?.step(step))
    return
  }
}
```

---

## 五、设计模式总结

| 模式 | 项目 | 实现方式 | 复用场景 |
|------|------|----------|----------|
| **组合模式** | Tiptap | StarterKit 组合多个扩展 | 插件化编辑器配置 |
| **注册表模式** | Quill | `Quill.register()` 统一注册 | 格式/模块/主题管理 |
| **缓存策略** | Quill | Block Blot 的 `cache` 对象 | 频繁计算的结果缓存 |
| **代理模式** | Tiptap | `Editor.view` 的 Proxy | 延迟初始化/空值安全 |
| **观察者模式** | Quill | `Emitter` 事件系统 | 编辑器事件通信 |
| **命令模式** | Tiptap | `addCommands()` 链式调用 | 编辑器操作抽象 |
| **策略模式** | doocs/md | `toggleFormat()` 通用化 | 格式切换的统一处理 |
| **管道模式** | Quill | Clipboard 的 matcher 数组 | HTML/Delta 转换链 |

---

## 六、可复用代码清单

| 优先级 | 来源 | 代码片段 | 复用场景 |
|--------|------|---------|---------|
| P0 | Tiptap | Vue 3 编辑器集成 | 核心编辑器 |
| P0 | doocs/md | 多图床管理器 | 图片上传 |
| P0 | Quill | Delta 格式转换器 | 内容适配 |
| P1 | doocs/md | `downloadFile()` | 文件下载 |
| P1 | doocs/md | `readingTime()` | 阅读时间计算 |
| P1 | doocs/md | `toggleFormat()` | 格式切换 |
| P1 | Quill | `sanitize()` | URL 安全检查 |
| P2 | Quill | 缓存策略 | 性能优化 |
| P2 | Tiptap | 事务批处理 | 性能优化 |

---

*报告生成时间：2026-06-29*
